const login = (data: { email: string; password: string }) => {
  console.log(data.email, data.password);
};

login({ email: 'dani@mail.com', password: '123' });

const add = (data: { email: string; password: string }) => {

}